# E-Commerce Database SQL Project

## 📌 Project Overview

This project is a relational database system for an e-commerce store, built using MySQL.

The project demonstrates database design, table relationships, data management, and SQL-based business analysis.

## 🛠️ Technologies Used

* MySQL
* SQL
* MySQL Workbench

## 🗄️ Database Structure

The database `myshop` contains the following tables:

* **customer** – Stores customer information
* **products** – Stores product details, prices, and stock
* **orders** – Stores customer orders and order status
* **order_items** – Stores products and quantities associated with each order
* **payments** – Stores payment information for orders

### Table Relationships

```text
Customer
   │
   └── Orders
          │
          ├── Order_Items ─── Products
          │
          └── Payments
```

## 📊 SQL Analysis Performed

The project includes SQL queries to analyze:

* Revenue generated by each product
* Top customers by total spending
* Best-selling products
* Number of cancelled orders

The queries use SQL concepts such as:

* `SELECT`
* `WHERE`
* `JOIN`
* `GROUP BY`
* `ORDER BY`
* Aggregate functions such as `SUM()` and `COUNT()`
* Foreign keys and relational database design

## 📁 Project Files

| File                      | Description                                                           |
| ------------------------- | --------------------------------------------------------------------- |
| `01_database.sql`         | Creates the database and tables                                       |
| `02_data_insertion.sql`   | Inserts sample customers, products, orders, order items, and payments |
| `03_analysis_queries.sql` | Contains SQL queries for business analysis                            |

## 🎯 Project Objective

The objective of this project is to demonstrate practical SQL and relational database skills by building an e-commerce database and extracting useful business insights from the data.

## 👨‍💻 Author

**Rudresh Jain**
